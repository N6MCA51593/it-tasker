import React from 'react';

const ScrollToDevicesBtn = ({ handleClick }) => {
  return (
    <div className='scroll-to-devices nav-btn' onClick={handleClick}></div>
  );
};

export default ScrollToDevicesBtn;
